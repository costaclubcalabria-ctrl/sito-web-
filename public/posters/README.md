# Poster di fallback

Un `.webp` per prodotto, con **la stessa inquadratura del render 3D**.

Si vedono al posto del canvas quando WebGL non è disponibile o l'utente ha
chiesto meno movimento, e sono anche le immagini Open Graph.

Come generarli: `MODELS.md` §4. Target: meno di 120 KB ciascuno.
